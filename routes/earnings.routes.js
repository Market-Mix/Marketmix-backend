const express = require('express');
const router = express.Router();

// Earnings routes placeholder
router.get('/', (req, res) => {
	res.json({ status: 'success', message: 'Earnings routes placeholder' });
});

module.exports = router;
