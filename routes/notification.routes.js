const express = require('express');
const router = express.Router();

// Notification routes placeholder
router.get('/', (req, res) => {
	res.json({ status: 'success', message: 'Notification routes placeholder' });
});

module.exports = router;
