const express = require('express');
const router = express.Router();

// Withdrawal routes placeholder
router.get('/', (req, res) => {
	res.json({ status: 'success', message: 'Withdrawal routes placeholder' });
});

module.exports = router;
